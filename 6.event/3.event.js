/*** */function Event(){};Event.prototype.addListener=function(type,listener){    if(typeof listener !='function'){        throw  TypeError('listener must be a function');    }    if(!this._events){        this._events={}    }    if(this._events[type]){       this._events[type].push(listener);    }else{        this._events[type]=[listener];    }};Event.prototype.emit=function(type){    if(!this._events[type]){        return;    }    var h =this._events[type];    var arr = new Array(arguments.length-1)    for(var i = 1; i<arguments.length;i++){        arr[i-1]=arguments[i];    }    for(var i = 0; i< h.length;i++){        h[i].apply(this,arr);    }}function Bell (){}Bell.prototype= new Event();var bell = new Bell();function Teacher (lesson){    console.log('老师来了上'+lesson+'课')}function student(lesson,nimei){    console.log('老师上你妹啊'+lesson+':'+nimei)}bell.addListener('ring',Teacher);bell.addListener('ring',student);bell.emit('ring','语文课','nimei');