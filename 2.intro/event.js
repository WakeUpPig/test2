/** $('button').click(function(){*   alert('clicked')* })** */// 加载核心包 样板间var http = require('http');//开一家自己的霍营庆丰包子店//q是权重var waiter = function(req,res){    var url = req.url;    res.writeHead(200,{"Content-Type":'text/html;charset=utf-8'});    console.log(url);    var path = url.split('?')[0];    var query = url.split('?')[1];    if(path == '/baozi'){        res.end(query.split('=')[1]+'包子');    }else if(path =='/doufu'){        res.end(query.split('=')[1]+'豆腐');    }else{        res.end('没有这个菜')    }    //console.log(req.url);    console.log(req.headers['accept-encoding']);    //setTimeout(function(){    //    res.write('can i help you');    //},1000)    //setTimeout(function(){    //    res.write('can i help you');    //},3000)    //setTimeout(function(){    //    res.end('can i help you');    //},5000)}var server = http.createServer(waiter);server.listen(3000);/** *  1xx 请求已经被受理，需要继续处理 *  2xx 服务器已经接受，正常响应。 *  3xx 重定向 需要客户端进一步。 *  4xx 客户端请求错误 *  5xx 服务器端错误 * * **/